import 'package:flutter/material.dart';

class JogoDaMemoriaPage extends StatelessWidget {
  const JogoDaMemoriaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("Jogo da memória"),
      ),
    );
  }
}
