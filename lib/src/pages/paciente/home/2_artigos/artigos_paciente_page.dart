import 'package:flutter/material.dart';

class ArtigosPacientePage extends StatelessWidget {
  const ArtigosPacientePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Artigo page"),
    );
  }
}
